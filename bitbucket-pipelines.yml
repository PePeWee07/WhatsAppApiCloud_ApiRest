image: docker:20.10.24

pipelines:
  branches:
    main:
      - step:
          name: Build & Push Docker Image
          runs-on:
            - self.hosted
            - linux
          services:
            - docker
          caches:
            - docker
          script:
            - echo "Building Docker image"
            - docker build -t pepewee/whatsapp-api-cloud:$VERSION .
            - docker tag pepewee/whatsapp-api-cloud:$VERSION pepewee/whatsapp-api-cloud:latest
            - echo "Logging in to Docker Hub"
            - echo $DOCKER_HUB_TOKEN | docker login -u $DOCKER_HUB_USER --password-stdin
            - echo "Pushing image to Docker Hub"
            - docker push pepewee/whatsapp-api-cloud:$VERSION
            - docker push pepewee/whatsapp-api-cloud:latest

      - step:
          name: Deploy to Server
          runs-on:
            - self.hosted
            - linux
          script:
            - pipe: atlassian/ssh-run:0.8.1
              variables:
                SSH_USER: "$SSH_USER"
                SERVER: "$SSH_HOST"
                SSH_KEY: "$SSH_KEY"
                COMMAND: |
                  set -e
                  echo "$DOCKER_HUB_TOKEN" > /tmp/token.txt
                  cat /tmp/token.txt | docker login -u "$DOCKER_HUB_USER" --password-stdin

                  echo "Pulling latest Docker image"
                  docker pull pepewee/whatsapp-api-cloud:latest

                  echo "Stopping and removing old containers"
                  docker stop whatsapp-api-cloud || true
                  docker rm whatsapp-api-cloud || true
                  docker stop whatsapp-api-cloud-db || true
                  docker rm whatsapp-api-cloud-db || true

                  echo "Running new containers"
                  cd /data/sites/ia-sp-backoffice.cue.ec/html/back-end
                  docker-compose up -d --remove-orphans

definitions:
  services:
    docker:
      memory: 2048
